 {% block body %}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

<div class="mt-4">
    <h1 class="text-center">Service</h1>
</div>
<table class="table container card d-flex justify-content-center align-items-center">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ service.id }}</td>
        </tr>
        <tr>
            <th>Amount</th>
            <td>{{ service.amount }}</td>
        </tr>
        <tr>
            <th>StartDate</th>
            <td>{{ service.startDate ? service.startDate|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>EndDate</th>
            <td>{{ service.endDate ? service.endDate|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Subject</th>
            <td>{{ service.subject }}</td>
        </tr>
        <tr>
            <th>Picture</th>
            <td><img style="max-width:35rem;" src="{{ service.picture }}" alt=""></td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ service.status ? 'Yes' : 'No' }}</td>
        </tr>
    </tbody>
</table>
<div class="d-flex justify-content-center gap-2 ">
    <a class="btn btn-secondary" style="height: 2.4rem;" href="{{ path('app_service_index') }}">back to list</a>

    <a class="btn btn-warning" style="height: 2.4rem;" href="{{ path('app_service_edit', {'id': service.id}) }}">edit</a> {{ include('service/_delete_form.html.twig') }}
</div> {% endblock %}